// Require the Express module and assign it to the variable 'express'
var express = require("express");

// Create a new router object from the Express module
var router = express.Router();

// Export the router object to make it available for use in other files
module.exports = router;
